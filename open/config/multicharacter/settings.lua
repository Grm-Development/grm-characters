return {
    -- The default number of character slots available to each player.
    -- This is the base amount before any purchased or special slots are added.
    defaultSlots = 1, 

    -- Enables GTA Online–style transitions when logging in and out.
    -- If disabled, the script will use a simple black screen instead.
    gtaoTransitions = true,

    -- When enabled, the script randomly selects a background from the list
    -- defined in open/config/multicharacter/templates.lua each time the character screen loads.
    -- When disabled, it uses the default background.
    randomBackground = true,

    -- The URL that opens when the player presses the "Purchase" button.
    -- Typically used for a store, Tebex link, Discord shop, or donation page.
    storeUrl = "https://discord.gg/bsepA3pmQP",
}